import { Prescriptionmedicines } from './prescriptionmedicines';

describe('Prescriptionmedicines', () => {
  it('should create an instance', () => {
    expect(new Prescriptionmedicines()).toBeTruthy();
  });
});
