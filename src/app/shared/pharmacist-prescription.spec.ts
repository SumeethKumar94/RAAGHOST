import { PharmacistPrescription } from './pharmacist-prescription';

describe('PharmacistPrescription', () => {
  it('should create an instance', () => {
    expect(new PharmacistPrescription()).toBeTruthy();
  });
});
